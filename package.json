{
  "name": "practice Typescript",
  "main": "src/index.html",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "deploy": "rm -rf src/dist/ && nmp build && gh-pages -d dist"
  },
  "homepage": "https://cornelialh.github.io/practiceTypescript/",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.2.1",
    "parcel": "^2.2.1",
    "typescript": "^4.5.4"
  }
}
